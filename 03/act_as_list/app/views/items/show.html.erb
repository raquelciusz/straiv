<div class="container">
  <h1 class="text-center"> <strong><%= @item.name.upcase %></strong></h1>
  <ul>
    <%= @item.id %>
    <%= @item.name %>
    <%= @item.position %>
  </ul>
  <div class="d-flex gap-3">
  <%= link_to "Destroy this item", @item, method: :delete, class: 'btn btn-primary mt-3' %>
  <%# <%= link_to "Update this list", edit_list_path(@list), class: 'btn btn-primary mt-3' %>
  </div>
</div>
